name: movie_flutter
description: AMKyawDev Recap App - Video subtitle editing application
publish_to: 'https://pub.dev'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  path_provider: ^2.1.1
  video_player: ^2.8.1
  flutter_subtitle: ^0.0.3
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/